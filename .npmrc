# NPM Configuration for Dependency Resolution
# Temporary workaround for zod version conflicts

# Use legacy peer dependency resolution to handle zod 3.x vs 4.x conflicts
legacy-peer-deps=true

# Auto-install peer dependencies to prevent manual intervention
auto-install-peers=true

# Set audit level to moderate to focus on important security issues
audit-level=moderate